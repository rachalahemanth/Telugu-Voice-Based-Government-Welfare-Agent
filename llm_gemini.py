import os
import google.generativeai as genai

# -------------------------------
# Configure Gemini
# -------------------------------
genai.configure(api_key="AIzaSyAvIk3QMAk8DKce8hwXocrdsgPf8MXjZ54")

model = genai.GenerativeModel(
    model_name="models/gemini-3-flash-preview",
    system_instruction="""
మీరు భారత ప్రభుత్వ సంక్షేమ పథకాలపై సహాయం చేసే Planner Agent.

నియమాలు:
- సమాధానాలు పూర్తిగా తెలుగులో ఉండాలి
- అర్హతపై ఖచ్చితంగా తెలియకపోతే ఊహించవద్దు
- అవసరమైన సమాచారం లేకపోతే వినియోగదారుని ప్రశ్నించండి
- సాధారణ ప్రజలకు అర్థమయ్యేలా వివరించండి
"""
)

# -------------------------------
# Planner Agent Function
# -------------------------------
def planner_agent_response(user_text: str) -> str:
    """
    Takes Telugu text (from STT) and generates a planner-style response.
    """

    prompt = f"""
వినియోగదారుడి అభ్యర్థన:
{user_text}

వినియోగదారుడికి సరైన ప్రభుత్వ పథకాల గురించి మార్గదర్శనం ఇవ్వండి.
"""

    response = model.generate_content(prompt)
    return response.text


# -------------------------------
# Example Run
# -------------------------------
if __name__ == "__main__":
    stt_output = "నాకు ప్రభుత్వ సంక్షేమ పథకానికి దరఖాస్తు చేయాలని ఆసక్తి ఉంది, కానీ నేను ఏ పథకానికి అర్హుడో నాకు స్పష్టంగా తెలియడం లేదు. దయచేసి నాకు సహాయం చేయండి."

    reply = planner_agent_response(stt_output)

    print("🧠 Planner Agent Response (Telugu):")
    print(reply)
